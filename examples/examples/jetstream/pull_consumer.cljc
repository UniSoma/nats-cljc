(ns examples.jetstream.pull-consumer
  "Pull Consumers
   Upstream: https://natsbyexample.com/examples/jetstream/pull-consumer/go
   Exercises: create-consumer (durable/ephemeral) + consume / next + acks.")

(defn -main [& _args]
  (println "examples.jetstream.pull-consumer: not implemented yet"))
