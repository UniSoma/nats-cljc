---
id: nts-01kstx8jppcc
title: Project scaffold + green CI skeleton
status: closed
type: chore
priority: 0
mode: afk
created: '2026-05-29T22:21:47.606541012Z'
updated: '2026-05-30T01:56:33.319121800Z'
closed: '2026-05-30T01:56:33.319121800Z'
acceptance:
- title: 'JVM build: `deps.edn` resolves `io.nats:jnats` 2.x + promesa and the `tools.build` script produces a jar'
  done: true
- title: 'CLJS build: `shadow-cljs` compiles the `:browser` and `:node` test targets'
  done: true
- title: '`deps.cljs` declares the npm dependency `@nats-io/nats-core`'
  done: true
- title: CI matrix runs JVM + Node + browser-headless jobs, each starting a `nats-server` with the websocket listener enabled (`ws://`)
  done: true
- title: CI is green on a trivial build across all three jobs
  done: true
- title: '`LICENSE` is Apache-2.0 and the build coordinate is `io.github.UniSoma/nats-cljc`'
  done: true
- title: The browser-headless CI job runs cljs.test under the shadow-cljs :karma target on headless Chrome (karma-chrome-launcher), green
  done: true
---

## Description

Stand up the build, dependency, and CI foundation so an empty library compiles and tests green on all three platforms — the substrate every later slice builds on. No NATS behavior yet.

Deliver: `deps.edn` (JVM deps incl. `io.nats:jnats` + promesa), `shadow-cljs.edn` (CLJS build + test targets), `deps.cljs` declaring the npm dep `@nats-io/nats-core`, a `tools.build` build script, a GitHub Actions CI matrix running JVM / Node / browser-headless against a downloaded `nats-server` binary with the **websocket listener enabled** (`ws://`), and an Apache-2.0 `LICENSE`.

The Clojars coordinate `io.github.UniSoma/nats-cljc` goes in the build config as-is. The `io.github.<owner>` segment is case-sensitive and permanent once published — verify it matches the GitHub org login exactly before the first publish (not a blocker for scaffolding).

**Pinned versions (researched 2026-05-29):**
- `io.nats/jnats` — `2.25.3` (latest 2.x; no 3.x exists yet)
- `funcool/promesa` — `11.0.678` (latest stable; do not pin the 12.0.0-RC2 pre-release)
- `org.clojure/clojurescript` — `1.12.145` (the release that adds native `^:async`/`await`, required by ADR 0002; this is the floor — do not go below it)
- `thheller/shadow-cljs` — `3.4.11` (the Clojars dep and the `shadow-cljs` npm package both at 3.4.11; keep them in lockstep)
- `@nats-io/nats-core` — `3.3.1` (npm)
- `nats-server` — `v2.14.1` (downloaded in CI)

**Browser-headless harness:** shadow-cljs `:karma` target driving headless Chrome via `karma-chrome-launcher` (npm devDeps: `karma`, `karma-chrome-launcher`, `karma-cljs-test`). Real-browser execution is the faithful environment for the `wsconnect` WebSocket transport. On GitHub Actions `ubuntu-latest` Chrome is preinstalled, so `ChromeHeadless` runs with no extra provisioning. CI step: `shadow-cljs compile test && karma start --single-run`.

ADRs: 0001 (transports), 0002 (CLJS >= 1.12.145 for native await), 0003 (toolchain — jnats + @nats-io/nats-core via shadow-cljs), 0009 (Apache-2.0, coordinate, semver contract).

## Notes

**2026-05-29T23:13:04.548139685Z**

> *This was generated by AI during triage.*

Pinned dependency versions in the description (verified via web search 2026-05-29) and specified the previously-open browser-headless harness as shadow-cljs :karma + ChromeHeadless. Sources: Maven Central / GitHub releases (jnats 2.25.3, nats-server v2.14.1), npm (@nats-io/nats-core 3.3.1, shadow-cljs 3.4.11), Clojars (promesa 11.0.678, shadow-cljs 3.4.11), clojurescript.org (1.12.145 release).

**2026-05-30T01:38:42.255223682Z**

License metadata for the pom (so Clojars/Maven display Apache-2.0, not just a LICENSE file on disk). When writing build.clj, pass :pom-data to tools.build write-pom:

  (b/write-pom {:class-dir class-dir
                :lib       (quote io.github.UniSoma/nats-cljc)
                :version   version
                :basis     basis
                :pom-data  [[:licenses
                             [:license
                              [:name "Apache-2.0"]
                              [:url  "https://www.apache.org/licenses/LICENSE-2.0.txt"]
                              [:distribution "repo"]]]]})

LICENSE (Apache-2.0, verbatim from apache.org) and NOTICE (Copyright 2026 UniSoma) already committed at repo root.

**2026-05-30T01:50:54.658043381Z**

Scaffold implemented and verified locally (clojure 1.12.5 / Java 25 / Node 24):
- clj -T:build jar -> target/nats-cljc-0.1.0-SNAPSHOT.jar; pom carries Apache-2.0 license + coordinate io.github.UniSoma/nats-cljc.
- clj -X:test green (1/1).
- shadow-cljs :node target compiles + 'node target/node-tests.js' green (1/1).
- shadow-cljs :browser (:karma) target compiles; output target/karma-test.js has shadow.test.karma init.
- clj-kondo clean (0/0). Remote org is github.com:UniSoma -> coordinate case matches.
Files: deps.edn, build.clj, shadow-cljs.edn, src/deps.cljs, package.json, karma.conf.js, ci/nats.conf, .github/workflows/ci.yml, src+test trivial ns, .gitignore.

Remaining 3 ACs (CI matrix runs; all-three green; browser-headless karma green on Chrome) need an actual CI run — local box has no Chrome and CI hasn't run yet. Pending first push.

**2026-05-30T01:56:33.319121800Z**

Scaffold + green CI shipped (merged to main as 46eb802). JVM (jnats 2.25.3 + promesa 11.0.678, tools.build jar with Apache-2.0 pom + io.github.UniSoma/nats-cljc), CLJS shadow-cljs :node + :browser/:karma targets, deps.cljs npm @nats-io/nats-core 3.3.1. 3-job CI matrix (jvm/node/browser-headless), each starting nats-server v2.14.1 with the ws:// listener; all green on GitHub. No NATS behavior yet — substrate for the tracer bullet.
