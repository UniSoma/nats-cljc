(ns nats-cljc.core
  "Portable public facade for nats-cljc (always aliased `nats`).

   Scaffold only — no NATS behavior yet (ticket nts-01kstx8jppcc). The real
   surface lands on top of the internal protocol described in ADR 0005.")

(def version
  "Current library version."
  "0.1.0-SNAPSHOT")
